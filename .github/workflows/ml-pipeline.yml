name : TRy Again 

on : 
  push : 
    branches : [main]

jobs : 
  validate : 
    runs-on : ubuntu-latest
    steps : 
      - name : Step1
        uses : actions/setup-python@v5
        with : 
          python-version : '3.10'
      
      - name : Step2 
        run : python -c "print('HEllo')" 

  deploy:
    runs-on: ubuntu-latest
    needs: validate
    steps:
      - name : Hello
        uses : actions/setup-python@v5
        with : 
          python-version : '3.10'
      
      - name : TRy_2
        run : |
          python -c "print('hello')"
          python -c "print('hello')"
          python -c "print('hello')"
          python -c "print('hello')"
          python -c "print('hello')"
          python -c "print('hello')"
